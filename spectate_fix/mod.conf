name = spectate_fix
description = Patches ObjectRef:get_yaw() to return get_look_horizontal() for players, fixing crashes when mcl_player processes players attached to other players (e.g. arena_lib spectate).
depends = mcl_player
